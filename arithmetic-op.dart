void main() {
  /** Example of Arithmetic Operators
  
  +	Add
  –	Subtract
  *	Multiply
  /	Divide
  ~/	Divide, returning an integer result
  %	Get the remainder of an integer division (modulo)
  ++ Increment
  -- Decrement
   
  */

  var a = 5;
  var b = 2;

  var sum = (a + b == 7);
  var subtract = (a - b == 3);
  var multiply = (a * b == 10);
  var divide = (a / b == 2.5); // Result in a Dobule
  var Divide = (a ~/ b == 2); // Result in a int
  var remainder = (a % b == 1); // Remainder

  print(sum); // Print all the variables one by one.
}
 
  


  
















